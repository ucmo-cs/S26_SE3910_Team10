package com.team10.appointments.model;

// Represents the lifecycle state of an appointment
public enum AppointmentStatus {
    SCHEDULED,  // appointment is active and upcoming
    CANCELLED   // appointment was cancelled by the user
}
